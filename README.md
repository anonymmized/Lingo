# Lingo

## Сборка

```bash
cmake -S . -B build
cmake --build build
```

## Запуск на macOS по ярлыку

После сборки в папке `build/` появится файл `Lingo.command`.
Его можно запускать двойным кликом из Finder или вынести на рабочий стол как ярлык.
